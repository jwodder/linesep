[tox]
envlist = py27,py35

[testenv]
usedevelop = True
deps =
    pytest~=3.0
    pytest-cov~=2.0
    pytest-flakes~=1.0
commands = pytest linesep.py test

[pytest]
addopts = --cache-clear --cov=linesep --cov-report term-missing --flakes
